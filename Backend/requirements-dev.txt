# Dependances necessaires uniquement pour executer la suite de tests.
# L'image Docker de production n'installe que requirements.txt.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# Les tests tournent sur SQLite en memoire : aucune base MySQL n'est requise.
-r requirements.txt
pytest==9.1.1
